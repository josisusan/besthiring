<!DOCTYPE html>
<html>
<head>
  <title>Besthiring</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<header>
  <div id="logo">
    <%= link_to "BestHiring", jobs_path %>
  </div>
  <div id="user_auth_nav">
    <% if admin_signed_in? %>
      <%= link_to "Edit Profile", edit_admin_registration_path %> |
      <%= link_to "Log Out", destroy_admin_session_path %>
    <% else %>
      <%= link_to "Log In", new_admin_session_path %> | 
      <%= link_to "Sign Up", new_admin_session_path %>
    <% end %>
  </div>
  <nav>
    <ul>
      <li>View Jobs</li>
      <% if admin_signed_in? %>
        <li><%= link_to "Add Jobs", new_job_path%></li>
      <% end %>
    </ul>
  </nav>
</header>
<div id = "notification">
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
</div>
<%= yield %>

</body>
</html>
